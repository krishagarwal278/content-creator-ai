/**
 * Dashboard Feature Barrel Export
 */

// Pages
export { default as DashboardPage } from "./DashboardPage";
export { default as HistoryPage } from "./HistoryPage";

// Components - import directly to avoid circular dependencies
// e.g., import { GenerationPanel } from "@/features/dashboard/generation-panel";
